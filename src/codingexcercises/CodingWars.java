package codingexcercises;

import java.util.Arrays;

public class CodingWars {

    public static void main(String[] args){
        System.out.println("a's value: " + Character.getNumericValue('a'));
        System.out.println("c's value: " + Character.getNumericValue('c'));
        System.out.println("m's value: " + Character.getNumericValue('m'));
        System.out.println("z's value: " + Character.getNumericValue('z'));

        System.out.println("A's value: " + Character.getNumericValue('A'));

    }

    public static String longestConsec(String[] strarr, int k) {
        if(k > strarr.length || k <= 0){
            return "";
        }
        return "";
    }
}
